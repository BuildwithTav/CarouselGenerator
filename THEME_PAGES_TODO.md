# Theme Pages — requested changes (not yet built)

- **Stop button during generation.** Once a batch starts (e.g. asked for
  10), there's no way to cancel or change your mind mid-run — you have to
  wait for it to finish. Add a Stop/Cancel button on the "generating"
  screen that halts the batch after the current post finishes and keeps
  whatever was already generated.
