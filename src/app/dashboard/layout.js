export const metadata = {
  title: "Content Dashboard — Build with Tav",
  description: "Brand and media command centre",
};

export default function DashboardLayout({ children }) {
  return children;
}
